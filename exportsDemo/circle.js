
//@ Durga Prasad
var util = require('./utility');
var radius=10;

console.log(util.circleArea(radius));
console.log(util.circlePerimeter(radius));

//the following function can't be used as it is not exported from the utility module
//console.log(getPiValue());

